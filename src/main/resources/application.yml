server:
    port: 8008
spring:
    application:
        name: config-service
    cloud:
        config:
            server:
                git:
                    default-label: config-server
                    uri: https://github.com/leave20/allpa-project-config-data
