server:
    port: 8008
spring:
    application:
        name: config-service
    cloud:
        config:
            server:
                git:
                    default-label: main
                    uri: https://github.com/leave20/allpa-project-config-data
                    search-paths: config-data