server:
    port: 8008
spring:
    profiles:
            active: git
    application:
        name: config-service
    cloud:
        config:
            server:
                git:
                    default-label: main
                    uri: https://github.com/leave20/allpa-project-config-data/config-data